# MILS – 作業 I
授課教授：許志鍾
助教長：李嘉明 與 吳明軒

## I. 資料集
- 資料集網址：https://cchsu.info/files/images.zip
- mini-ImageNet資料集是ImageNet資料集的一個縮小版本，旨在減少計算複雜度同時保留大型資料集的基本特性。
- 資料的組織方式如下：
  - train.txt：包含訓練圖像的檔案路徑和標籤。
  - validation.txt：包含驗證圖像的檔案路徑和標籤。
  - test.txt：包含測試圖像的檔案路徑和標籤。
- 驗證效能：在訓練期間，您必須使用驗證集來評估您的模型。報告驗證集上的效能指標（例如準確率、損失）以監控模型的效能並調整超參數。
- 最終效能：完成訓練後，使用測試集評估您的模型。基於測試集報告最終效能指標。這確保報告的效能反映模型泛化到未見數據的能力。

## II. 任務A：為可變輸入通道設計卷積模組
### 描述：
設計一個特殊的卷積模組，該模組在空間大小上具有不變性，並能處理任意數量的輸入通道。您只需設計這個特殊模組，而不是CNN的每一層。設計完成後，解釋設計原則、參考文獻、額外成本（如FLOPS或參數數量），並與樸素模型進行比較。為了模擬您方法的實用性，使用ImageNet-mini資料集進行訓練，並在推理過程中測試具有各種通道組合的圖像（如RGB、RG、GB、R、G、B等），然後比較效能。

### 詳細示例：
1. 設計原則：
   動態卷積：設計一個可以根據輸入通道數量調整其權重的動態卷積模組。這可以通過學習一個權重生成網絡來實現，該網絡將輸入通道作為輸入並生成相應的卷積核。

2. 實施詳情：
   - 卷積模組：使用動態權重生成網絡。這個網絡接收輸入通道的數量並相應地生成卷積核。這些核可以用於標準卷積操作。
   - 參考文獻：動態卷積的概念可以參考Wu等人在CVPR 2020發表的論文《Dynamic Convolution: Attention over Convolution Kernels》。成本分析：與樸素模型相比，這種設計可能會增加額外的計算成本（如FLOPS）和參數，但可以顯著減少為不同通道數量訓練和存儲模型的需求。

3. 實驗設計：
   訓練和推理：使用ImageNet-mini資料集進行訓練，並在推理期間在具有各種通道組合的圖像上測試模型。比較：比較使用動態卷積模組的模型與樸素模型在不同輸入通道組合中的效能，評估準確性和計算成本。

## III. 任務B：設計用於圖像分類的兩層網絡
### 描述：
使用CNN、Transformer或RNN架構設計具有2到4個有效層的網絡，該網絡在ImageNet-mini資料集上達到ResNet34模型效能的至少90%（即，效能下降不超過10%）。對參數數量或FLOPS沒有限制；但是，您架構中的輸入和輸出層數不得超過4到6層。

### 重要說明：
在本作業中，有效層被定義為單個基本單元（例如，卷積層、GCN層或RNN單元）。如果您決定使用複合塊（例如，RRDB或類似結構），這些塊內部由多個操作組成，您必須通過詳細說明每個塊的結構來明確指定有效層數。

### 指示：
- 設計策略：
  - 感受野增強：
    納入自注意力機制、圖卷積網絡（GCN）或其他方法，有效增加網絡的感受野。
  - 架構策略：
    您可以考慮使用
    或ResNeXt的思想來加寬過濾器，但確保您的設計符合有效層數。

- 實施詳情：
  - 架構描述：
    清晰描述您的網絡架構。解釋您認為什麼是一個有效層，如果使用複合塊，請提供詳細的分解。
  - 基準比較：
    從頭開始在ImageNet-mini資料集上訓練ResNet34作為基準。避免使用任何預訓練的ResNet34模型。
  - 資源考量：
    雖然對參數數量或計算成本沒有明確限制，但包括對這些方面的討論，將您的網絡與基準進行比較。

- 文檔和報告：
  - 設計原理：
    提供對您設計原則的清晰解釋，適當引用相關文獻。
  - 層定義：
    明確定義在您的架構中構成一個有效層的內容。如果使用複合塊，詳細說明其內部結構和產生的有效層數。
  - 實驗發現：
    呈現定量和定性結果，並包括任何有助於證明您設計選擇的消融研究。

## IV. 評估準則
- 理論論證（30%）
  - 提供對所提出解決方案的清晰連貫解釋，強調設計如何滿足任務要求。證明所提出方法中的設計選擇和假設。討論所提出解決方案與傳統方法相比的潛在優勢和局限性。

- 實驗驗證（40%）
  - 實施所提出的解決方案並進行實驗以驗證其有效性。從準確性、計算成本（如FLOPS）或兩者方面比較所提出方法與基準模型的效能。提供量化指標來支持聲明，如準確率、精確度、召回率、F1分數或計算成本。呈現展示模型視覺質量或其他相關效能指標的定性結果。分析實驗結果並討論與基準方法相比觀察到的改進或權衡。

- 消融研究和分析（30%）
  - 進行消融研究以調查所提出解決方案中不同組件或超參數的影響。改變關鍵參數，如卷積模組的結構、層數或注意力機制，並評估它們對效能的影響。基於消融研究提供見解和解釋，證明所選擇的配置。

- 注意：
  本作業的重點是證明設計的卷積模組和兩層網絡的有效性，而不是達到最先進的效能。所提出的解決方案應該相較於傳統方法顯示在準確性、計算成本或兩者方面的改進。解決方案中的聲明應該由理論論證和實驗驗證支持。評估準則強調為所提出方法提供清晰理論論證、進行徹底實驗以驗證其有效性，並以組織良好且易於理解的方式呈現工作的重要性。您需要提供定量和定性結果，進行消融研究以分析不同組件的影響，並討論與基準方法相比觀察到的改進或權衡。通過滿足這些評估準則，您可以展示對卷積模組和兩層網絡設計和實施的理解，以及進行嚴格實驗和分析的技能。

## V. 提交要求
### 4.1. GitHub儲存庫（50%）：
- 創建一個GitHub儲存庫來託管您的實現和相關文件。
- 包括有詳細文檔和組織良好的代碼實現您的解決方案。
- 在README.md文件中提供關於如何運行代碼和重現實驗的清晰說明。
- 使用適當的git提交消息和分支來追踪開發進度。
- 確保儲存庫對授課教師和助教可訪問。
- 使用markdown格式的Readme或者提供Docker映像以確保可重現性。

### 4.2. 報告（50%）：
- 撰寫一份全面描述您的解決方案、實驗和發現的報告。
- 報告應該是您選擇的格式（例如，PDF、Markdown、LaTeX）並可以用任何您喜歡的語言撰寫。

### 4.3. 提交截止日期：
- GitHub儲存庫鏈接和報告應通過Moodle提交。
- 逾期提交將受到課程逾期提交政策的約束。
